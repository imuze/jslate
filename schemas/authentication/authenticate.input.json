{
    "title": "Token",
    "description": "This endpoint permits a registered user to authenticate.",
    "method": "POST",
    "route": "/tokens",
    "type": "object",
    "properties": {
        "token": {
            "properties": {
                "email": {
                    "type": "string"
                },
                "password": {
                    "type": "string"
                }
            },
            "required": ["email", "password"]
        }
    },
    "required": ["token"]
}
